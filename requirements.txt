# [Core Framework]
# 최소 2.4.0 이상을 사용하되, 시스템 환경(CUDA/MPS)에 맞는 최적 버전을 설치
torch>=2.4.0
torchvision>=0.19.0
torchaudio>=2.4.0

# [Vision Library]
timm>=1.0.0
segment-anything>=1.0
segmentation-models-pytorch>=0.3.0

# [Medical Image Processing]
# 고정 버전보다는 호환 범위를 지정하여 OS별 설치 유연성 확보
opencv-python>=4.8.0
scikit-image>=0.22.0
# numpy 2.0은 기존 라이브러리와 충돌이 잦으므로 1.x 시리즈 마지막 안정판 권장
numpy>=1.24.0,<2.0.0
albumentations>=1.4.0

# [Utilities]
scikit-learn>=1.3.0
tqdm>=4.65.0
pandas>=2.0.0
matplotlib>=3.8.0
# 맥(M1/M2) 사용자를 위해 pycocotools는 버전을 강제하지 않는 것이 좋음
pycocotools>=2.0.0